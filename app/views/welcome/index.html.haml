.content
  .page-header
    %h1
      Timonin
    %small Небольшой сайт
  -if user_signed_in?
    .post-button
    =link_to 'Написать', new_post_path, class: "btn btn-xs btn-primary"
  .row
    - @posts.each do |post|
      %h2= post.title
      = post.body
    %br
    = paginate @posts
  .row
    %h1
      Reserved for photoes
